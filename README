For the database migrations the next commands have to be excecuted:

When you have downloaded the flyway configuration files
Zodra de SQL File en flyway configuration files gedownload zijn met versiebeheer kan er via de windows CMD de volgende acties worden voltooid:
Eerst open je de CMD, en vervolgens set je de path van de CMD naar de map waar flyway.cmd in staat:

BIJV. C:\Users....\Medtracker\src\db

Na naar deze map te zijn genavigeerd kan je de volgende command uitvoeren om de migration te doen:

flyway migrate

